<%= form_with(model: eduardo, local: true) do |form| %>
  <% if eduardo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eduardo.errors.count, "error") %> prohibited this eduardo from being saved:</h2>

      <ul>
      <% eduardo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :eduardo_nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :eduardo_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
